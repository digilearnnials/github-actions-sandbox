namespace Digi.EditorTools
{
    public enum ProductType
    {
        Client,
        Server
    }

    public enum EnvironmentType
    {
        Development,
        Staging,
        Production
    }

    public enum ServerType
    {
        Hosted,
        Local
    }
}